<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="indi.wyx0k.story.service.image.mapper.ImageInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="indi.wyx0k.story.service.image.entity.ImageInfo">
        <id column="id" property="id" />
        <result column="description" property="description" />
        <result column="title" property="title" />
        <result column="locationX" property="locationX" />
        <result column="locationY" property="locationY" />
        <result column="emotion" property="emotion" />
        <result column="wkid" property="wkid" />
        <result column="tag" property="tag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, description, title, locationX, locationY, emotion, wkid, tag
    </sql>

</mapper>
